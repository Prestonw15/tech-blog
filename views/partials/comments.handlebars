<div class="comments">
    {{#each this}}
    <section class="comments">
        <div class="meta">
            {{user.username}} on {{format_date created at}}
        </div>
        <div class="text">
            {{text}}
        </div>
    </section>

</div>
