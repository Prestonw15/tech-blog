<ol class="post-list">
    {{#each posts as |post|}}
    <li>
        {{> post-info post }}
    </li>
    {{/each}}
</ol>

{{#if loggedIn}}
<div>
    <a href="/dashboard"><button type="button">New Post</button></a>
</div>

{{/if}}